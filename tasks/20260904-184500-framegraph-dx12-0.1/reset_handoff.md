# Reset handoff

Current PACT: 00. Main is not yet committed. Worktree contains initial contract and baseline files. No GPU process is owned by this task.

Read AGENTS.md, product_contract.md and progress.md first. Next command: `mqb run tests/unit/smoke.cpp --std 23 --debug --no-discover` from the repository root. Complete baseline verification before commit/main push. Then fresh-fetch and create `feat/framegraph-dx12-0.1` from origin/main. Resume at the first unchecked task; do not create another repository.
