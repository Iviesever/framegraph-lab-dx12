# Reset handoff

Current phase: PACT-70 is complete; Tier 2 performance/memory measurement is next. Base/main exact SHA: `fb23f40818661ed791854181f33ee22bf6ee57fc`. PACT-70 implementation SHA: `984c228c0d6a4829f9fdb81e4538d6ce2a57a788`. Feature branch: `feat/framegraph-dx12-0.1`. Clean exact receipt: `artifacts/checkpoints/pact70.json`. The current branch may contain a successor documentation/evidence commit; always fresh-fetch and compare `git rev-parse HEAD` with `origin/feat/framegraph-dx12-0.1`. No GPU/browser/server process is owned by this task.

Read AGENTS.md, product_contract.md and progress.md first. Fresh-fetch origin and compare local HEAD with origin/feat/framegraph-dx12-0.1. Next: read evidence/pact30-contract.md, create runtime CLI/unit acceptance and a failing runtime stub, then implement context/diagnostics/options/main for clear/present. Core is green (70 unit cases, CTest 6/6 in four profiles, 10k valid + 10k invalid including ASan/UBSan, bounded fuzz). Portable toolchain is `.tools/llvm-mingw-20260826-ucrt-x86_64/bin`; prepend to process PATH only. MSVC wrapper is scripts/with-msvc.ps1. All scratch should use artifacts/tmp. Respect one GPU process and check unrelated UE/GPU activity before long GPU work. Resume at first incomplete gate; do not create another repository.

Use scripts/build.ps1 for daily Windows C++ builds and runs. Add new C++ files ONLY through build-manifest.json, regenerate mqb.json with python tools/build_contract.py --write, and keep CMake consuming the same targets. CMake/CTest now include build-contract/negative gates (8/8 tested). Runtime executable target must be added to manifest when it exists. Updated freeze is 2026-09-05 12:00 JST; source-only future release and reset instructions are in the contract.

PACT-30 supersedes the old next-command paragraph: the app target now exists. MQB app Debug/Release, hardware/WARP clear/present, visible resize/minimize/restore, CLI and shader negatives are green. Next read evidence/pact40-contract.md, write placed-resource executor/readback parity RED acceptance, then implement arena/barrier emitter/access guard/capture. Existing runtime uses temporary manual clear transitions solely for PACT-30; replace them with the Core plan in PACT-40. Preserve Core and build-contract tests.

PACT-40 supersedes the preceding next step: executor/arena/capture/timestamps and hardware/WARP parity are green. Next read evidence/pact50-contract.md, add procedural shaders/renderer and a scene validation RED test, then make Neon Ruins the default Graph Program. Keep probe selectable internally for focused executor regression, or invoke its builder directly in tests; do not let the solid probe become the final capture. Update manifest assets and local/CMake post-build copy before runtime shader loading.

PACT-50 supersedes the preceding next step: Neon Ruins, WARP/hardware scene parity, updated Core sweep and graph-aware resize are green. Next read evidence/pact60-contract.md, generate the real static inspector from the clean primary plan/report/capture, check JSON consistency and perform browser desktop/narrow/selection/highlight/console QA. Preserve exact PACT-50 primary paths for viewer input; do not hardcode sample data.

PACT-60 supersedes the preceding next step: inspector generation/static/browser QA and checked real sample are complete. Begin PACT-80 contracts/docs/CI/scripts. Do not regenerate primary artifacts until final source/docs/audit fixes are committed; then build from that clean exact HEAD, run all stress/parity/package checks, and write ignored final receipts. A fresh second read-only audit agent is reserved for the final origin/main...HEAD whole-diff review.

PACT-80 preparation is ready to commit: boundary tests, all required docs, CI and scripts/verify/package/validate-package. After push, run `./scripts/verify.ps1` only from a clean tree. It serializes GPU runs and writes its exact summary under artifacts/verification/<sha>. Do not mark P0 complete until all steps, CI, independent audit and Draft PR pass.

The first local full run at cb592de passed all 21 steps, but remote Ubuntu Clang failed on missing library `std::expected`. Follow-up rejected a conditional fallback ABI. Final fix must use unconditional project-owned Expected, fail-fast:false and install consumer. After committing: rerun scripts/verify.ps1 from clean HEAD, wait for green CI, request final fix approval, then create Draft PR. Older packages are historical.

PACT-70 supersedes the preceding next step: Core IndirectArgument state, compute frustum culling, stable visible IDs, ExecuteIndirect, CPU reference count/pixel parity and graph-managed culling transitions passed on WARP and hardware at the implementation SHA above. Do not repeat PACT-00/10/20 or P0. Next create a measured Tier 2 benchmark/stress contract, benchmark compile/allocation/barrier and alias on/off memory/GPU/CPU record values, and run 100,000 bounded graphs. Change production code only for a demonstrated hotspot. Then run Tier 3 reliability, full clean verification/package, remote CI, PR update and final audit.
