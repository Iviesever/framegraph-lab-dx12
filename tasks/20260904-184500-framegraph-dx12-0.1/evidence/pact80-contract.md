# PACT-80 P0 closure contract before delivery changes

Finish every original/supplemental P0 gate without adding a visual feature. Add the six reviewer boundary tests (invalid enums/states, imported WAR/UAV chains, conservative overwrite retention, real cycle pairs, exact usage and edge bounds). Keep MQB primary and CMake/CTest/MSVC/Clang/GCC/sanitizer required.

Create all required portfolio docs and CI, then one fail-closed full verification script and local package script. Full validation is serial for GPU processes: clean rebuilds; 10k+10k property; bounded fuzz; canonical repeat; hardware interactive; WARP/hardware parity; Debug 0/0/0; 1,000-frame WARP and hardware; resize/minimize/restore; all negatives; inspector regeneration/static check; no residual process.

Package only from a clean exact HEAD. Generate Win64/source ZIP, inner and outer manifests and SHA-256. Extract to a fresh ignored directory and run the exact 240-frame WARP capture/report/plan command. Require shader lookup inside the extraction, parsed/valid/nonempty outputs, real alias event, matching plan identity/provenance and Debug 0/0/0. Do not upload binaries or create a release/tag.

Use the reserved second fresh read-only agent to audit origin/main...HEAD and ignored final receipts. Fix confirmed High/Blocker and low-risk directly related Medium only, then recertify if HEAD changes. Create/update a Draft PR with exact base/head, complete matrix, local artifact paths/sizes/SHA, limitations/source-only policy and AI statement. Do not merge.

Only after all P0/package/debug/parity/Draft PR baseline gates are green, and before the updated PACT-70 cutoff/freeze, begin the separately contracted GPU-driven culling tier.
